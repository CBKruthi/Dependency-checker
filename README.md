# CrewAI Repo Dependency Scanner

This project uses **CrewAI** agents to:

1. Clone a Bitbucket repository (public or private).
2. Extract **Go dependencies** (direct & indirect).
3. (Optional) Generate structured JSON reports.

## 🚀 Run

```bash
python crew_project.py
```
